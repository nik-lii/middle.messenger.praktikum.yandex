import {BaseBlock} from "../../blocks/baseBlock/baseBlock";
import template from "./authorization.hbs";
import "./login.less";

export class Authorization extends BaseBlock {
  constructor() {
    super();
  }

  render() {
    // return this.compile(template, {});
  }
}
